<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="@drawable/card_background_selector"
    android:descendantFocusability="afterDescendants">

    <TextView android:id="@+id/sort_value"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:gravity="center_vertical"
        android:text="sample sort"
        android:layout_marginLeft="@dimen/margin_x_small"
        android:textSize="16dp"/>

    <ImageView android:id="@id/reorder"
        android:layout_width="32dp"
        android:layout_height="match_parent"
        android:layout_toLeftOf="@+id/remove"
        android:src="@drawable/reorder"
        android:layout_marginRight="@dimen/margin_x_small"
        android:layout_centerVertical="true"/>

    <ImageView android:id="@id/remove"
        android:layout_width="32dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:src="@drawable/delete"
        android:layout_marginRight="@dimen/margin_x_small"
        android:layout_centerVertical="true"/>


</RelativeLayout>
